import './globals.css';
export const metadata = { title: 'PayRound Owner - Admin Control', description: 'Owner dashboard to control PayRound user site' };
export default function RootLayout({ children }) { return (<html lang="en"><body>{children}</body></html>); }
